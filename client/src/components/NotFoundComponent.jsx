export default function NotFound() {
// i added that in the SinglePageComponent, so this component will never be called
    return (
      <p className="lead">Nothing to see here... This is not the route you are looking for!</p>
    );
  }